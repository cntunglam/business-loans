import test from "node:test";

test("Utils function", async () => {
  // await test("Birthdate to age calculation is correct", async () => {
  //   assert.equal(birthDateToAge(2006, 5, 17), 18);
  //   assert.equal(birthDateToAge(2006, 5, 18), 17);
  // });
});
